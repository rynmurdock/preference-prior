
[] filter / pad / etc. cases of short < k lists of scores.
[] setup hparam sweeps
  [x] update config to dataclass
  [] sweep diffusion schedule?
[x] unify the qual_val and app.py code.
[] clean up the whole codebase.
    it's a bit of a mess.

[] diffusion vs eclipse-style MSE ablation.
  [x] test changes
  [x] update loss calc
  [x] update prior pipe
  [x] add back timestep conditioning

